# Leave empty when the API is served from the same origin (recommended for Render).
# For a split Vercel + Render deploy, set the public API URL:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=/api
